# IMPLEMENTATION REPORT

## 実施内容

- 既存スターターコードを確認し、Django 5.2 構成、モデル、判定エンジン、管理画面、テンプレート、テストを把握しました。
- 判定エンジンを、`approved_at` が登録された承認済み `RuleVersion` のみ本番判定へ使う形に変更しました。
- 承認済みルールの適用期間重複を入力・設定エラーとして検出する処理を追加しました。
- 提出予定一覧に、複数予定を一括で提出済みにする画面操作を追加しました。
- 提出実績CSV取込を追加しました。拡張子、サイズ、列名、日付形式を検証し、照合できない行は未反映件数として通知します。
- CSV出力に数式インジェクション対策を追加しました。
- 初期ルール投入コマンドで、導入直後に判定できるよう `approved_at` を設定するようにしました。
- ローカル確認用に `seed_demo_data` 管理コマンドを追加しました。デモ法人、施設、利用者、加算設定、提出予定を作成できます。
- 月中に利用者加算が開始した場合も、その対象月の個別機能訓練・ADL予定を生成できるよう、月単位の対象判定に修正しました。
- 画面内に「使い方」ページを追加し、毎月の確認手順、初期登録項目、CSV取込列、状態の見方を確認できるようにしました。
- 提出実績CSVのひな形ダウンロードを追加しました。
- 外部公開前チェック画面と `check_deployment_readiness` 管理コマンドを追加しました。
- READMEにDockerなしの確認手順、提出実績CSV取込、バックアップ・復元、承認済みルール運用を追記しました。

## 検証結果

- `python manage.py check`: 成功
- `python manage.py makemigrations --check --dry-run`: 成功、差分なし
- `python manage.py migrate --noinput`: 成功
- `python manage.py load_official_rules`: 成功
- `python manage.py seed_demo_data`: 成功
- `pytest`: 8件成功
- Djangoテストクライアントによるデモログイン後ダッシュボード表示: 成功
- in-app browserでデモログイン、提出予定一覧表示、個別機能訓練・ADL維持等加算の表示確認: 成功
- in-app browserで使い方ページ表示、提出実績CSVひな形リンク表示: 成功
- 公開前チェック画面とCLIコマンドの実行確認: 成功

## 追加した主なテスト

- 未承認ルールが提出予定作成に使われないこと
- 再計算が冪等で、提出済み履歴が保持されること
- LIFE登録人数不一致が入力・設定エラーとして出ること
- 他施設の提出予定IDを直接指定して提出済み登録できないこと
- 月中開始の利用者加算が対象月の提出予定として生成されること

## 未実施事項

- この実行環境では `docker` コマンドがPATHに存在しなかったため、`docker compose up --build` によるコンテナ起動検証は未実施です。
- パスワードリセット、アカウントロック、TOTP多要素認証はMVP外の追加開発として残しています。
- LIFE制度ルールは令和6年3月15日通知を根拠候補にした初期実装です。本番導入前に最新通知、Q&A、自治体・保険者解釈を制度担当者が確認し、承認者と承認日時を記録してください。

## 今後の追加開発候補

- ルール旧版・新版の画面比較
- 施設・サービス・加算別の入退院、利用再開後の承認済み例外ルール
- CSV取込プレビューと行別エラー表示
- 管理者向けアカウントロック、TOTP多要素認証
- 本番運用向けのバックアップ復元リハーサル手順書
